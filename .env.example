# ==========================================================================
# TEMPLATE DE CONFIGURAÇÕES (TEMPLATE.env)
# ==========================================================================
# Este arquivo serve como um guia de quais informações o sistema precisa.
# ⚠️ NÃO COLOQUE SENHAS REAIS AQUI. 
# -> Copie este arquivo, renomeie para '.env' e preencha lá.
# ==========================================================================

# --- 1. CONFIGURAÇÕES DO DJANGO ---
# DEBUG=True  -> apenas em DESENVOLVIMENTO (portal-ggci-dev).
# DEBUG=False -> OBRIGATÓRIO em MAIN e PRODUÇÃO. Com True, uma exceção devolve
#   ao visitante a tela amarela do Django, com trechos de código e variáveis de
#   ambiente. Também desliga o manifest do Whitenoise: sem ele os arquivos
#   estáticos não recebem hash no nome e o navegador serve JS/CSS vencidos.
# Atenção: o .env viaja no rsync de MAIN para PROD (portal.sh, opção 5), então
#   o valor que vale em produção é o do MAIN.
DEBUG=True
SECRET_KEY='gerada-automaticamente-pelo-setup'
ALLOWED_HOSTS=*

# --- 2. BANCO DE DADOS LOCAL (MYSQL) ---
# Usado para armazenar usuários do portal e configurações do sistema.
DB_NAME='portal_ggci'
DB_USER='portal_user'
DB_PASSWORD='sua_senha_local'
DB_HOST='127.0.0.1'
DB_PORT='3306'

# --- 3. DASHBOARD POLICHAT ---
# Credenciais para extração automática de dados do painel Polichat.
DASHBOARD_POLICHAT_USER_PROD='seu_email_prod@ovg.org.br'
DASHBOARD_POLICHAT_PASS_PROD='sua_senha_prod'
DASHBOARD_POLICHAT_USER_DEV='seu_email_dev@ovg.org.br'
DASHBOARD_POLICHAT_PASS_DEV='sua_senha_dev'

# --- 4. PORTAL PBU (GOVERNO) ---
# O Portal PBU possui dois níveis de acesso diferentes usados pela IA.
PORTAL_PBU_USER='seu_usuario'
PORTAL_PBU_PASS_ANALISE_IA='senha_para_analise_ia'
PORTAL_PBU_PASS_PAGAMENTOS='senha_para_pagamentos'

# --- 5. BANCO DE DADOS EXTERNO (SIBU) ---
# Conexão direta com o banco de dados oficial SIBU para auditoria.
SIBU_BANCO_DADOS_HOST='10.237.1.16'
SIBU_BANCO_DADOS_USER='seu_usuario_sibu'
SIBU_BANCO_DADOS_PASS='sua_senha_sibu'
SIBU_BANCO_DADOS_NAME='sibu'
